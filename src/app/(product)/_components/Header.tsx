import { Button } from "antd"

export const Header = () => {
    return (
        <div className="text-right w-full p-4">
            <Button type="primary" danger>Logout</Button>
        </div>
    )
} 